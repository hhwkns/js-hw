const firstName = 'Heather';
const lastName = 'Dileepan';
const age = 38;
const drivingAge = 16;

if (age >= drivingAge) {
    console.log(firstName + ' ' + lastName + ' ' + 'has been driving for'+ ' ' + (age - drivingAge) + ' ' + 'years');
} else {
  console.log('countdown');
}
